@inherits GenericPageArgsBase

@if (PageArgs != null)
{
    <GenericModelTabsView T="Permission" TRender="PermissionRender"
               PageArgs="@PageArgs"
               IdentityField="PermissionId" TitleField="Name"
               GetEndpoint="@AtlasAPIEndpoints.GET_PERMISSION"
               CreateEndpoint="@AtlasAPIEndpoints.CREATE_PERMISSION"
               UpdateEndpoint="@AtlasAPIEndpoints.UPDATE_PERMISSION"
               DeleteEndpoint="@AtlasAPIEndpoints.DELETE_PERMISSION" />
}