@inherits GenericPageArgsBase

@if (PageArgs != null)
{
    <GenericModelsView 
        T="Module"
        @rendermode="new InteractiveServerRenderMode()"
        PageArgs="@PageArgs" 
        Endpoint="@AtlasAPIEndpoints.GET_MODULES" />
}