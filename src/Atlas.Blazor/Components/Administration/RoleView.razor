@inherits GenericPageArgsBase

@if (PageArgs != null)
{
    <GenericModelTabsView T="Role" TRender="RoleRender"
               PageArgs="@PageArgs"
               IdentityField="RoleId" TitleField="Name"
               GetEndpoint="@AtlasAPIEndpoints.GET_ROLE"
               CreateEndpoint="@AtlasAPIEndpoints.CREATE_ROLE"
               UpdateEndpoint="@AtlasAPIEndpoints.UPDATE_ROLE"
               DeleteEndpoint="@AtlasAPIEndpoints.DELETE_ROLE" />
}