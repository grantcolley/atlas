@inherits GenericPageArgsBase

@if (PageArgs != null)
{
    <GenericModelsView T="Module" @rendermode="new InteractiveServerRenderMode(/*prerender: false*/)" PageArgs="@PageArgs" Endpoint="@AtlasAPIEndpoints.GET_MODULES" />
}