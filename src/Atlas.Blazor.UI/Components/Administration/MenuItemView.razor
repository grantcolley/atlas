@inherits GenericPageArgsBase

@if (PageArgs != null)
{
    <GenericModelView T="MenuItem" TRender="MenuItemRender"
               PageArgs="@PageArgs"
               IdentityField="MenuItemId" TitleField="Name"
               GetEndpoint="@AtlasAPIEndpoints.GET_MENU_ITEM"
               CreateEndpoint="@AtlasAPIEndpoints.CREATE_MENU_ITEM"
               UpdateEndpoint="@AtlasAPIEndpoints.UPDATE_MENU_ITEM"
               DeleteEndpoint="@AtlasAPIEndpoints.DELETE_MENU_ITEM" />
}